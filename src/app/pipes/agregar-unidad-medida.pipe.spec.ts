import { AgregarUnidadMedidaPipe } from './agregar-unidad-medida.pipe';

describe('AgregarUnidadMedidaPipe', () => {
  it('create an instance', () => {
    const pipe = new AgregarUnidadMedidaPipe();
    expect(pipe).toBeTruthy();
  });
});
