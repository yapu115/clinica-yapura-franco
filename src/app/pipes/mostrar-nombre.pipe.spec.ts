import { MostrarNombrePipe } from './mostrar-nombre.pipe';

describe('MostrarNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new MostrarNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
