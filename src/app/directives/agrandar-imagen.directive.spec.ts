import { AgrandarImagenDirective } from './agrandar-imagen.directive';

describe('AgrandarImagenDirective', () => {
  it('should create an instance', () => {
    const directive = new AgrandarImagenDirective();
    expect(directive).toBeTruthy();
  });
});
