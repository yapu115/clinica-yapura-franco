import { EnviarMailDirective } from './enviar-mail.directive';

describe('EnviarMailDirective', () => {
  it('should create an instance', () => {
    const directive = new EnviarMailDirective();
    expect(directive).toBeTruthy();
  });
});
