import { MostrarCardDirective } from './mostrar-card.directive';

describe('MostrarCardDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarCardDirective();
    expect(directive).toBeTruthy();
  });
});
